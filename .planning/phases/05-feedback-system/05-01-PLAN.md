---
phase: 05-feedback-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/FeedbackDisplay.tsx
  - src/lib/feedback/sounds.ts
  - public/sounds/correct.mp3
  - public/sounds/hint.mp3
autonomous: true

must_haves:
  truths:
    - "Correct answer shows green checkmark within 0.5 seconds"
    - "Incorrect answer shows gentle visual cue (not harsh red X)"
    - "Audio cue plays on correct answer (optional, can be muted)"
  artifacts:
    - path: "src/components/FeedbackDisplay.tsx"
      provides: "Visual feedback component with checkmark/gentle cue"
      exports: ["FeedbackDisplay"]
    - path: "src/lib/feedback/sounds.ts"
      provides: "Audio playback utility"
      exports: ["playCorrectSound", "playHintSound"]
  key_links:
    - from: "FeedbackDisplay.tsx"
      to: "sounds.ts"
      via: "import and call on state change"
      pattern: "playCorrectSound|playHintSound"
---

<objective>
Create visual feedback component (checkmark for correct, gentle indicator for incorrect) with optional audio cues.

Purpose: FEED-01 requires <0.5sec visual feedback, FEED-05 requires no harsh corrections
Output: FeedbackDisplay component + audio utilities ready to integrate
</objective>

<execution_context>
@C:\Users\limfe\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\limfe\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/components/PracticeSession.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create audio utility</name>
  <files>src/lib/feedback/sounds.ts, public/sounds/</files>
  <action>
Create `src/lib/feedback/sounds.ts`:
- Export `playCorrectSound()` and `playHintSound()` functions
- Use Web Audio API for instant playback (no load delay)
- Generate simple tones programmatically: correct = happy rising tone (C5->E5, 150ms), hint = gentle single tone (G4, 100ms)
- Include `setMuted(muted: boolean)` and `isMuted()` for user preference
- Store mute preference in localStorage key "feedback-audio-muted"
- Fallback gracefully if AudioContext unavailable (no errors, just silent)

Why programmatic audio: Avoids asset loading delays, ensures <0.5sec requirement met.
  </action>
  <verify>
Create quick test: `import { playCorrectSound } from '@/lib/feedback/sounds'; playCorrectSound();` in browser console should play tone.
  </verify>
  <done>Both sound functions work, mute toggle persists across refresh</done>
</task>

<task type="auto">
  <name>Task 2: Create FeedbackDisplay component</name>
  <files>src/components/FeedbackDisplay.tsx</files>
  <action>
Create `src/components/FeedbackDisplay.tsx`:

Props interface:
```typescript
interface FeedbackDisplayProps {
  result: "correct" | "incorrect" | "hint" | null;
  message?: string;
  onAnimationEnd?: () => void;
}
```

Component behavior:
- `result="correct"`: Green checkmark (use SVG), scale-in animation (0.3s), trigger playCorrectSound()
- `result="incorrect"`: Orange/amber circle with "?" (not X, not red - FEED-05), trigger playHintSound()
- `result="hint"`: Same as incorrect but lighter styling (for hint levels 2-3)
- `result=null`: Render nothing
- Display `message` text below icon when provided
- Call onAnimationEnd after 0.5s for auto-dismiss timing
- Animations: CSS keyframes, scale + opacity

Styling:
- Checkmark: green-500 (#22c55e), bold stroke
- Incorrect/hint: amber-500 (#f59e0b) for gentle visual
- Text: gray-700, encouraging tone
- Fixed height container (h-16) to prevent layout shift
  </action>
  <verify>
Storybook or direct render test: <FeedbackDisplay result="correct" message="Great job!" /> shows green checkmark with text
  </verify>
  <done>Component renders all 4 states correctly with animations under 0.5s</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` passes
- [ ] FeedbackDisplay renders checkmark for correct
- [ ] FeedbackDisplay renders amber "?" for incorrect (not red X)
- [ ] Audio plays on correct (when not muted)
- [ ] No console errors
</verification>

<success_criteria>
- Green checkmark appears for correct answers
- Gentle amber indicator for incorrect (no harsh red X per FEED-05)
- Audio cues work and respect mute setting
- All animations complete within 0.5 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/05-feedback-system/05-01-SUMMARY.md`
</output>
