---
phase: 05-feedback-system
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - src/components/PracticeSession.tsx
  - src/components/HintButton.tsx
autonomous: false

must_haves:
  truths:
    - "Student sees green checkmark within 0.5s of correct answer"
    - "Student sees 'Try again' hint on first incorrect (not 'Wrong')"
    - "Student can request up to 3 hints before seeing animated explanation"
    - "Student sees encouraging message after both correct and corrected answers"
    - "Student never sees negative language or point deductions"
  artifacts:
    - path: "src/components/PracticeSession.tsx"
      provides: "Integrated feedback system in practice flow"
      contains: ["FeedbackDisplay", "useHints", "HintButton"]
    - path: "src/components/HintButton.tsx"
      provides: "Button to request next hint level"
      exports: ["HintButton"]
  key_links:
    - from: "PracticeSession.tsx"
      to: "FeedbackDisplay.tsx"
      via: "renders FeedbackDisplay on answer"
      pattern: "<FeedbackDisplay"
    - from: "PracticeSession.tsx"
      to: "useHints.ts"
      via: "hook call"
      pattern: "useHints\\("
    - from: "PracticeSession.tsx"
      to: "AnimatedFractionBar.tsx"
      via: "visual explanation when hints exhausted"
      pattern: "hintsExhausted.*AnimatedFractionBar|<AnimatedFractionBar"
---

<objective>
Wire all feedback components into PracticeSession: immediate feedback display, hint progression, visual explanation when hints exhausted, encouraging messages throughout.

Purpose: Complete FEED-01 through FEED-05 integration
Output: Fully functional feedback system in practice mode
</objective>

<execution_context>
@C:\Users\limfe\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\limfe\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-feedback-system/05-01-SUMMARY.md
@.planning/phases/05-feedback-system/05-02-SUMMARY.md
@src/components/PracticeSession.tsx
@src/components/AnimatedFractionBar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HintButton component</name>
  <files>src/components/HintButton.tsx</files>
  <action>
Create `src/components/HintButton.tsx`:

Props:
```typescript
interface HintButtonProps {
  currentLevel: HintLevel | null;
  hintsExhausted: boolean;
  onRequestHint: () => void;
  disabled?: boolean;
}
```

Behavior:
- Shows "Need a hint?" when currentLevel is null (first hint available)
- Shows "Another hint? ({3 - currentLevel} left)" when hints used but not exhausted
- Hidden or disabled when hintsExhausted is true
- Large touch target (min-h-[48px], px-6)
- Styling: secondary/outline button, not distracting from main answer buttons
- disabled prop for when answer is being processed
  </action>
  <verify>
Render HintButton with currentLevel=1, verify it shows "Another hint? (2 left)"
  </verify>
  <done>Button renders correctly for all states and is touch-friendly</done>
</task>

<task type="auto">
  <name>Task 2: Integrate feedback into PracticeSession</name>
  <files>src/components/PracticeSession.tsx</files>
  <action>
Modify `src/components/PracticeSession.tsx`:

**Add imports:**
- FeedbackDisplay from ./FeedbackDisplay
- HintButton from ./HintButton
- useHints from @/hooks/useHints
- getEncouragingMessage, getCorrectionMessage from @/lib/feedback/messages
- AnimatedFractionBar (already imported via BenchmarkComparisonView, may need direct import)

**Add state:**
```typescript
const [feedbackResult, setFeedbackResult] = useState<"correct" | "incorrect" | null>(null);
const [feedbackMessage, setFeedbackMessage] = useState<string | null>(null);
const [showExplanation, setShowExplanation] = useState(false);
```

**Initialize useHints:**
```typescript
const { currentLevel, hintsExhausted, currentHint, requestHint, resetHints } = useHints(
  currentProblem.type,
  currentProblem.fractionA,
  currentProblem.fractionB
);
```

**Modify handleAnswer:**
1. On correct: setFeedbackResult("correct"), setFeedbackMessage(getEncouragingMessage()), setTimeout to next problem
2. On incorrect (first attempt): setFeedbackResult("incorrect"), auto-call requestHint() to show first hint
3. On subsequent incorrect: just update feedbackResult, don't auto-advance hint (let user click HintButton)
4. When hintsExhausted and incorrect again: setShowExplanation(true), after explanation setFeedbackMessage(getCorrectionMessage())

**Modify generateNewProblem:**
- Call resetHints()
- Reset feedbackResult, feedbackMessage, showExplanation to null/false

**UI changes:**
- Replace existing feedback div with <FeedbackDisplay result={feedbackResult} message={feedbackMessage || currentHint} />
- Add HintButton below answer buttons (only visible when incorrect and !hintsExhausted)
- When showExplanation true: render animated bar comparison (reuse AnimatedFractionBar logic from BenchmarkComparisonView)
- After explanation animation completes: show CorrectionMessage, then auto-advance to next problem after 2s

**Remove:**
- Old lastResult state
- Old "Correct!" / "Try again!" text display

**Timing requirements:**
- Feedback shows within 0.5s of answer (immediate setState)
- After correct: 1.5s pause then next problem
- After explanation: 2s pause with message then next problem
  </action>
  <verify>
1. Click correct answer -> green checkmark + encouraging message appears within 0.5s
2. Click incorrect -> amber indicator + first hint text
3. Click HintButton -> next hint appears
4. After 3 incorrect + 3 hints -> animated explanation shows
5. After explanation -> encouraging correction message, then next problem
  </verify>
  <done>Full feedback flow works: immediate feedback, graduated hints, visual explanation, encouraging messages</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete feedback system with graduated hints and encouraging messages</what-built>
  <how-to-verify>
1. Run `npm run dev` and go to practice mode (any mode, any difficulty)
2. Answer correctly - verify:
   - Green checkmark appears within 0.5 seconds
   - Encouraging message shown (e.g., "Great job!")
   - Audio cue plays (if not muted)
   - Advances to next problem after ~1.5s

3. Answer incorrectly - verify:
   - Amber "?" indicator appears (NOT red X)
   - First hint text appears (method-specific)
   - "Try again" message, not "Wrong"

4. Click "Need a hint?" button - verify:
   - Second hint appears with more specific guidance
   - Button updates to show "Another hint? (1 left)"

5. Use all 3 hints and still answer wrong - verify:
   - Animated bar explanation appears
   - After animation: encouraging correction message (e.g., "Now you've got it!")
   - Advances to next problem

6. Verify no negative language anywhere:
   - No "Wrong", "Incorrect", "Failed", etc.
   - All messages are encouraging/positive
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- [ ] `npm run build` passes
- [ ] Correct answer shows green checkmark + encouraging message
- [ ] Incorrect answer shows amber indicator + hint (not "Wrong")
- [ ] Hint button allows requesting up to 3 hints
- [ ] After 3 hints exhausted, visual explanation shows
- [ ] Correction message is encouraging ("You're learning!")
- [ ] No negative language anywhere in UI
- [ ] All feedback appears within 0.5 seconds
</verification>

<success_criteria>
All 5 FEED requirements satisfied:
1. FEED-01: Immediate correctness indicator (<0.5 sec)
2. FEED-02: Graduated hint system (3 levels before reveal)
3. FEED-03: Visual explanation when hints exhausted
4. FEED-04: Encouraging messages throughout
5. FEED-05: No harsh corrections or negative language
</success_criteria>

<output>
After completion, create `.planning/phases/05-feedback-system/05-03-SUMMARY.md`
</output>
