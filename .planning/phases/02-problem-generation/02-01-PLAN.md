---
phase: 02-problem-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/fraction.ts
  - src/lib/fractions/constants.ts
  - src/lib/fractions/utils.ts
autonomous: true

must_haves:
  truths:
    - "Difficulty enum with Easy/Medium/Hard exists"
    - "P3 denominator set (2,3,4,5,6,8) is defined as constant"
    - "Problem type enum exists for 3 comparison methods"
    - "Fraction comparison utility returns correct ordering"
  artifacts:
    - path: "src/types/fraction.ts"
      provides: "Difficulty, ProblemType, ComparisonProblem types"
      contains: "Difficulty"
    - path: "src/lib/fractions/constants.ts"
      provides: "P3_DENOMINATORS, EASY_DENOMINATORS"
      exports: ["P3_DENOMINATORS", "EASY_DENOMINATORS"]
    - path: "src/lib/fractions/utils.ts"
      provides: "compareFractions, toDecimal, areEqual"
      exports: ["compareFractions", "toDecimal", "areEqual"]
  key_links:
    - from: "src/lib/fractions/utils.ts"
      to: "src/types/fraction.ts"
      via: "imports Fraction type"
      pattern: "import.*Fraction.*from"
---

<objective>
Create foundation types, constants, and utilities for problem generation.

Purpose: Establish type-safe foundation that all generators will use.
Output: Extended types, P3 denominator constants, fraction comparison utilities.
</objective>

<execution_context>
@C:\Users\limfe\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\limfe\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/types/fraction.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend fraction types</name>
  <files>src/types/fraction.ts</files>
  <action>
Add to existing fraction.ts:

1. Difficulty enum: 'easy' | 'medium' | 'hard'
2. ProblemType enum: 'same-numerator' | 'same-denominator' | 'different-both'
3. ComparisonProblem interface:
   - id: string
   - type: ProblemType
   - difficulty: Difficulty
   - fractionA: Fraction
   - fractionB: Fraction
   - correctAnswer: 'A' | 'B' | 'equal' (which is larger)

Export all new types.
  </action>
  <verify>TypeScript compiles: `npx tsc --noEmit`</verify>
  <done>Types exported and used in subsequent tasks without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create constants file</name>
  <files>src/lib/fractions/constants.ts</files>
  <action>
Create new file with:

1. P3_DENOMINATORS = [2, 3, 4, 5, 6, 8] as const (PROB-02)
2. EASY_DENOMINATORS = [2, 3, 4] as const (PROB-03: halves, thirds, quarters)
3. MEDIUM_DENOMINATORS = [2, 3, 4, 5, 6] as const
4. HARD_DENOMINATORS = P3_DENOMINATORS (all allowed)

Export all constants.
  </action>
  <verify>File exists and exports: `grep -l "P3_DENOMINATORS" src/lib/fractions/constants.ts`</verify>
  <done>Constants available for import in generator functions</done>
</task>

<task type="auto">
  <name>Task 3: Create fraction utilities</name>
  <files>src/lib/fractions/utils.ts</files>
  <action>
Create utility functions:

1. toDecimal(f: Fraction): number
   - Returns f.numerator / f.denominator

2. compareFractions(a: Fraction, b: Fraction): -1 | 0 | 1
   - Returns -1 if a < b, 0 if equal, 1 if a > b
   - Use decimal comparison (sufficient for P3 fractions)

3. areEqual(a: Fraction, b: Fraction): boolean
   - True if same decimal value (handles equivalent fractions)

4. isValidFraction(f: Fraction): boolean
   - numerator >= 0
   - denominator > 0
   - numerator <= denominator (proper fractions only for P3)

Import Fraction from types. Export all functions.
  </action>
  <verify>TypeScript compiles: `npx tsc --noEmit`</verify>
  <done>Utilities available for use in generators</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. Types can be imported: `import { Difficulty, ProblemType } from '@/types/fraction'`
3. Constants accessible: P3_DENOMINATORS has 6 values
4. Utils work: compareFractions({numerator:1,denominator:2}, {numerator:1,denominator:3}) returns 1
</verification>

<success_criteria>
- Difficulty enum has 3 values (easy, medium, hard)
- ProblemType enum has 3 values (same-numerator, same-denominator, different-both)
- P3_DENOMINATORS constant equals [2,3,4,5,6,8]
- EASY_DENOMINATORS constant equals [2,3,4]
- compareFractions returns correct ordering for any two fractions
</success_criteria>

<output>
After completion, create `.planning/phases/02-problem-generation/02-01-SUMMARY.md`
</output>
