---
phase: 01-visual-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tailwind.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/components/FractionBar.tsx
  - src/types/fraction.ts
autonomous: true

must_haves:
  truths:
    - "Next.js app runs with npm run dev"
    - "FractionBar component renders a horizontal bar with configurable partitions"
    - "Shaded segments are visually distinct from unshaded"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies"
      contains: "next"
    - path: "src/components/FractionBar.tsx"
      provides: "Single fraction bar rendering"
      min_lines: 40
    - path: "src/types/fraction.ts"
      provides: "Fraction type definitions"
      exports: ["Fraction", "FractionBarProps"]
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/FractionBar.tsx"
      via: "import and render"
      pattern: "import.*FractionBar"
---

<objective>
Create Next.js project with Tailwind CSS and implement FractionBar component that renders a horizontal bar with configurable partitions.

Purpose: Establish foundation for all visual fraction representation. Singapore Math uses horizontal bars exclusively.
Output: Running app with FractionBar component showing partitioned bar with shading capability.
</objective>

<execution_context>
@C:\Users\limfe\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\limfe\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project with Tailwind</name>
  <files>
    package.json
    tailwind.config.ts
    src/app/layout.tsx
    src/app/page.tsx
    src/app/globals.css
  </files>
  <action>
    Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --no-import-alias` in project root.

    After initialization:
    1. Clean up default page.tsx - remove boilerplate, keep minimal layout
    2. Set up base globals.css with Tailwind directives only
    3. Update layout.tsx metadata: title "Fraction Comparison Trainer"

    Note: Use App Router (--app flag), TypeScript, and Tailwind. No additional UI libraries needed for MVP.
  </action>
  <verify>
    Run `npm run dev` and confirm localhost:3000 loads without errors.
    Check package.json contains "next", "react", "tailwindcss".
  </verify>
  <done>
    Next.js app runs successfully with Tailwind CSS configured.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create FractionBar component</name>
  <files>
    src/types/fraction.ts
    src/components/FractionBar.tsx
    src/app/page.tsx
  </files>
  <action>
    Create types file with:
    ```typescript
    export interface Fraction {
      numerator: number;
      denominator: number;
    }

    export interface FractionBarProps {
      fraction: Fraction;
      shadedSegments?: number[]; // indices of shaded segments (0-based)
      width?: number;  // px, default 400
      height?: number; // px, default 60
    }
    ```

    Create FractionBar component:
    - Use SVG for crisp rendering at any scale (not canvas - SVG simpler for rectangles)
    - Render horizontal bar divided into `denominator` equal segments
    - Full bar shows complete whole (Singapore Math requirement VIS-02)
    - Segments at indices in `shadedSegments` array are filled with accent color
    - Unshaded segments have light background with visible borders
    - Border between segments must be visually obvious (VIS-03 equal partitions)

    Style requirements (ADHD-friendly from research):
    - High contrast: dark borders (#333), light unshaded (#f5f5f5), distinct shaded (#3b82f6)
    - Clean lines, no decorative elements
    - Segment borders clearly visible (1-2px solid)

    Update page.tsx to render a test FractionBar showing 2/5 (2 shaded segments out of 5).
  </action>
  <verify>
    npm run dev shows page with horizontal bar divided into 5 equal segments, 2 segments shaded blue.
    Segments are clearly separated by visible borders.
  </verify>
  <done>
    FractionBar renders horizontal bar with configurable partitions and shading. Test fraction 2/5 displays correctly.
  </done>
</task>

</tasks>

<verification>
- `npm run dev` starts without errors
- Browser shows fraction bar at localhost:3000
- Bar has 5 equal segments with 2 shaded
- Segment borders clearly visible
- No visual clutter (ADHD-friendly)
</verification>

<success_criteria>
1. Next.js + Tailwind project initialized and running
2. FractionBar component accepts fraction prop and renders correctly
3. Shaded vs unshaded segments visually distinct
4. Equal partitions clearly visible
</success_criteria>

<output>
After completion, create `.planning/phases/01-visual-foundation/01-01-SUMMARY.md`
</output>
