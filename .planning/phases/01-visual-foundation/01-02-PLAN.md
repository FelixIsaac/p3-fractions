---
phase: 01-visual-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/FractionBar.tsx
  - src/components/ComparisonView.tsx
  - src/app/page.tsx
  - src/hooks/useInteraction.ts
autonomous: true

must_haves:
  truths:
    - "Student can click a segment to toggle its shading"
    - "Student can drag across segments to shade multiple at once"
    - "Touch interactions work same as mouse interactions"
    - "Two bars display side-by-side for comparison"
  artifacts:
    - path: "src/hooks/useInteraction.ts"
      provides: "Unified mouse/touch interaction handling"
      exports: ["useInteraction"]
      min_lines: 30
    - path: "src/components/ComparisonView.tsx"
      provides: "Side-by-side fraction bar layout"
      min_lines: 25
  key_links:
    - from: "src/components/FractionBar.tsx"
      to: "src/hooks/useInteraction.ts"
      via: "hook usage for events"
      pattern: "useInteraction"
    - from: "src/components/ComparisonView.tsx"
      to: "src/components/FractionBar.tsx"
      via: "renders two FractionBars"
      pattern: "FractionBar.*FractionBar"
---

<objective>
Add interactive shading (click/drag for mouse, tap/drag for touch) to FractionBar and create ComparisonView for side-by-side fraction display.

Purpose: Enable hands-on manipulation (critical for ADHD engagement) and visual comparison (core learning activity).
Output: Interactive fraction bars that respond to click/tap/drag on both laptop and tablet.
</objective>

<execution_context>
@C:\Users\limfe\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\limfe\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-visual-foundation/01-01-SUMMARY.md
@.planning/research/PEDAGOGY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add interactive shading to FractionBar</name>
  <files>
    src/hooks/useInteraction.ts
    src/components/FractionBar.tsx
    src/types/fraction.ts
  </files>
  <action>
    Create useInteraction hook that unifies mouse and touch handling:
    - Tracks pointer position during drag
    - Returns handlers: onPointerDown, onPointerMove, onPointerUp
    - Uses PointerEvent API (works for both mouse and touch natively)
    - Tracks "isDragging" state for drag-to-shade behavior

    Update FractionBarProps to include:
    ```typescript
    onSegmentChange?: (segments: number[]) => void;
    interactive?: boolean; // default false for display-only mode
    ```

    Update FractionBar component:
    - When interactive=true, attach pointer event handlers to each segment
    - Click/tap toggles single segment shading
    - Drag across segments shades all segments in drag path (additive, not toggle)
    - Manage internal state for shaded segments OR use controlled mode via props
    - Use CSS touch-action: none on interactive bars to prevent scroll interference

    Implementation notes:
    - SVG elements support pointer events natively
    - Calculate which segment from pointer position using segment width
    - Set pointer-events: all on rect elements
  </action>
  <verify>
    On localhost:3000:
    - Click a segment: it toggles shaded/unshaded
    - Click and drag: all segments touched become shaded
    - Test with Chrome DevTools device emulation (touch mode)
  </verify>
  <done>
    FractionBar responds to click, tap, and drag interactions. Segments shade on interaction.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ComparisonView layout</name>
  <files>
    src/components/ComparisonView.tsx
    src/app/page.tsx
  </files>
  <action>
    Create ComparisonView component:
    - Displays two FractionBars side-by-side vertically (stacked, not horizontal)
    - Props: leftFraction, rightFraction, optional labels
    - Each bar has same width for visual comparison (VIS-04 requirement)
    - Clean layout with fraction labels below each bar (e.g., "2/5")
    - Responsive: bars stack vertically, full width on mobile, max-width on desktop

    Layout structure:
    ```
    [          Bar 1          ]
         2/5

    [          Bar 2          ]
         3/5
    ```

    Style requirements:
    - Centered in viewport
    - Clear spacing between bars (gap-8 or similar)
    - Fraction labels: large, readable (text-2xl), below their bar
    - Max-width: 600px on desktop, full width on tablet/mobile
    - ADHD-friendly: single focus area, no surrounding clutter

    Update page.tsx:
    - Replace test FractionBar with ComparisonView
    - Show comparison of 2/5 vs 3/5 (same denominator example)
    - Both bars interactive
  </action>
  <verify>
    On localhost:3000:
    - Two bars displayed vertically, centered
    - Fraction labels visible below each bar
    - Both bars respond to click/drag
    - Resize browser: layout remains clean at tablet and laptop widths
  </verify>
  <done>
    ComparisonView displays two interactive fraction bars side-by-side with labels. Layout works on different screen sizes.
  </done>
</task>

</tasks>

<verification>
- Click any segment: toggles shade
- Drag across segments: shades multiple
- Touch emulation in DevTools works same as mouse
- Two bars visible, same width, properly labeled
- Responsive from 768px (tablet) to 1440px (laptop)
</verification>

<success_criteria>
1. Click/tap toggles individual segment shading
2. Drag/swipe shades multiple segments
3. Touch works same as mouse (pointer events)
4. Two bars display side-by-side with fraction labels
5. Layout responsive for tablet (10") and laptop (13")
</success_criteria>

<output>
After completion, create `.planning/phases/01-visual-foundation/01-02-SUMMARY.md`
</output>
