---
phase: 03-core-practice-modes
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/app/page.tsx
autonomous: false

must_haves:
  truths:
    - "Student sees mode selection on app load"
    - "Student can select mode then difficulty then start practicing"
    - "Student can exit practice and return to selection"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Integrated practice flow"
      contains: "ModeSelector"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/ModeSelector.tsx"
      via: "renders ModeSelector"
      pattern: "<ModeSelector"
    - from: "src/app/page.tsx"
      to: "src/components/DifficultySelector.tsx"
      via: "renders DifficultySelector"
      pattern: "<DifficultySelector"
    - from: "src/app/page.tsx"
      to: "src/components/PracticeSession.tsx"
      via: "renders PracticeSession when ready"
      pattern: "<PracticeSession"
---

<objective>
Wire all components into main page with complete user flow.

Purpose: Connect selectors to practice session creating the full student experience (MODE-01 to MODE-05).
Output: Working practice mode selection and session flow.
</objective>

<execution_context>
@C:\Users\limfe\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\limfe\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-practice-modes/03-01-SUMMARY.md
@.planning/phases/03-core-practice-modes/03-02-SUMMARY.md
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate components into page</name>
  <files>src/app/page.tsx</files>
  <action>
Refactor page.tsx to implement the full practice flow.

State:
- selectedMode: ProblemType | "mixed" | null
- selectedDifficulty: Difficulty | null
- isPlaying: boolean

Flow (3 screens):
1. Mode Selection Screen (isPlaying=false, mode=null):
   - Title: "Fraction Comparison Trainer"
   - Subtitle: "Choose a practice mode"
   - ModeSelector component
   - When mode selected: show difficulty selector

2. Difficulty Selection Screen (isPlaying=false, mode set, difficulty=null):
   - Title: "Fraction Comparison Trainer"
   - Selected mode shown as badge/label
   - Subtitle: "Choose difficulty"
   - DifficultySelector component
   - "Back" button to return to mode selection
   - When difficulty selected: auto-start (set isPlaying=true)

3. Practice Screen (isPlaying=true):
   - PracticeSession component with mode + difficulty
   - onExit: set isPlaying=false, clear selections

Remove the hardcoded ComparisonView demo.

Layout:
- Centered content, max-w-2xl, padding for mobile
- Clean transitions between screens
- Consistent header across all screens
  </action>
  <verify>
npm run dev - app loads without errors
TypeScript compiles: npx tsc --noEmit
  </verify>
  <done>Page shows mode selection, difficulty selection, and practice session flow</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete practice mode selection and session flow</what-built>
  <how-to-verify>
1. Run: npm run dev
2. Open http://localhost:3000
3. Verify Mode Selection:
   - See 4 mode buttons (Same Numerator, Same Denominator, Different Both, Mixed)
   - Click "Same Numerator" - button shows selected state
4. Verify Difficulty Selection:
   - See 3 difficulty buttons (Easy, Medium, Hard)
   - See "Back" button to return to mode selection
   - Click "Easy"
5. Verify Practice Session:
   - See two fraction bars with same numerator problem
   - See A/B labels on bars
   - See 3 answer buttons
   - Click correct answer - see brief feedback
   - New problem loads after delay
6. Verify Exit:
   - Click exit/back - returns to selection screen
7. Test Mixed mode:
   - Go back, select Mixed mode + any difficulty
   - Verify different problem types appear
8. Test on narrow viewport (mobile simulation):
   - Buttons remain touch-friendly
   - Layout adapts
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. npm run dev - starts without errors
2. npx tsc --noEmit - no type errors
3. All 5 requirements (MODE-01 to MODE-05) functionally working
</verification>

<success_criteria>
- MODE-01: Same Numerator mode works (select, practice, answer)
- MODE-02: Same Denominator mode works
- MODE-03: Different Both mode works
- MODE-04: Mixed mode randomly selects problem types
- MODE-05: Easy/Medium/Hard difficulty selection works for all modes
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-practice-modes/03-03-SUMMARY.md`
</output>
