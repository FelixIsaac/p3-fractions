---
phase: 03-core-practice-modes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/PracticeSession.tsx
  - src/components/AnswerButtons.tsx
autonomous: true

must_haves:
  truths:
    - "Student sees current problem with two fraction bars"
    - "Student can click A, B, or Equal to answer"
    - "New problem loads after answer"
  artifacts:
    - path: "src/components/PracticeSession.tsx"
      provides: "Practice session container managing problem state"
      exports: ["PracticeSession"]
    - path: "src/components/AnswerButtons.tsx"
      provides: "Answer input buttons"
      exports: ["AnswerButtons"]
  key_links:
    - from: "src/components/PracticeSession.tsx"
      to: "src/lib/fractions/generators.ts"
      via: "imports generateProblem"
      pattern: "import.*generateProblem.*from"
    - from: "src/components/PracticeSession.tsx"
      to: "src/components/ComparisonView.tsx"
      via: "renders ComparisonView"
      pattern: "<ComparisonView"
    - from: "src/components/PracticeSession.tsx"
      to: "src/components/AnswerButtons.tsx"
      via: "renders AnswerButtons"
      pattern: "<AnswerButtons"
---

<objective>
Create practice session component with problem display and answer input.

Purpose: Orchestrate the core practice loop - show problem, accept answer, generate next problem.
Output: PracticeSession and AnswerButtons components ready for page integration.
</objective>

<execution_context>
@C:\Users\limfe\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\limfe\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/types/fraction.ts
@src/lib/fractions/generators.ts
@src/components/ComparisonView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AnswerButtons component</name>
  <files>src/components/AnswerButtons.tsx</files>
  <action>
Create a client component for answer selection.

Props:
- onAnswer: (answer: "A" | "B" | "equal") => void
- disabled?: boolean

Layout:
- Flex row with gap-4, justify-center
- Three large buttons

Buttons:
1. "A is bigger" -> calls onAnswer("A")
2. "B is bigger" -> calls onAnswer("B")
3. "Equal" -> calls onAnswer("equal")

Styling (Tailwind):
- Each button: px-8 py-4 text-lg font-semibold rounded-xl
- bg-white border-2 border-gray-300
- Hover: border-blue-500, bg-blue-50
- Active: bg-blue-100
- Focus-visible: ring-2 ring-blue-500 ring-offset-2
- Disabled: opacity-50 cursor-not-allowed
- Min touch target: min-h-[56px] min-w-[100px]

Use "use client" directive.
  </action>
  <verify>TypeScript compiles: npx tsc --noEmit</verify>
  <done>AnswerButtons renders 3 answer options and calls callback on click</done>
</task>

<task type="auto">
  <name>Task 2: Create PracticeSession component</name>
  <files>src/components/PracticeSession.tsx</files>
  <action>
Create a client component that manages the practice session.

Props:
- mode: ProblemType | "mixed"
- difficulty: Difficulty
- onExit?: () => void

State:
- currentProblem: ComparisonProblem | null
- lastResult: "correct" | "incorrect" | null (for brief feedback)

Behavior:
1. On mount and when mode/difficulty changes: generate new problem
2. For "mixed" mode: randomly select from ["same-numerator", "same-denominator", "different-both"]
3. On answer: compare with correctAnswer, show brief feedback, generate next problem after 800ms delay

Layout (vertical stack, centered):
1. Exit button (top-left, small, "Back" or X icon)
2. Current mode + difficulty label (e.g., "Same Numerator - Easy")
3. ComparisonView showing fractionA (top) and fractionB (bottom)
4. Label "A" above top bar, "B" above bottom bar
5. AnswerButtons below the bars
6. Brief feedback text (optional, e.g., "Correct!" or "Try again")

Use generateProblem from @/lib/fractions/generators.
Import ComparisonView from @/components/ComparisonView.
Import AnswerButtons from @/components/AnswerButtons.
Import types from @/types/fraction.

Disable AnswerButtons during the 800ms feedback delay.
  </action>
  <verify>TypeScript compiles: npx tsc --noEmit</verify>
  <done>PracticeSession generates problems and handles answer flow with feedback</done>
</task>

</tasks>

<verification>
1. npx tsc --noEmit - no type errors
2. PracticeSession imports and uses generateProblem
3. PracticeSession renders ComparisonView with problem fractions
4. AnswerButtons callback correctly typed
</verification>

<success_criteria>
- AnswerButtons shows 3 clearly labeled buttons
- PracticeSession generates appropriate problem on mount
- Mixed mode randomly selects problem type
- Answer triggers feedback then new problem
- All touch targets >= 44px
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-practice-modes/03-02-SUMMARY.md`
</output>
