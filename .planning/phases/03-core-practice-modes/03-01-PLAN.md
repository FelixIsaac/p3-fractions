---
phase: 03-core-practice-modes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ModeSelector.tsx
  - src/components/DifficultySelector.tsx
autonomous: true

must_haves:
  truths:
    - "Student sees 4 mode buttons: Same Numerator, Same Denominator, Different Both, Mixed"
    - "Student sees 3 difficulty buttons: Easy, Medium, Hard"
    - "Selected mode/difficulty have distinct visual state"
  artifacts:
    - path: "src/components/ModeSelector.tsx"
      provides: "Mode selection UI with 4 options"
      exports: ["ModeSelector"]
    - path: "src/components/DifficultySelector.tsx"
      provides: "Difficulty selection UI with 3 options"
      exports: ["DifficultySelector"]
  key_links:
    - from: "src/components/ModeSelector.tsx"
      to: "@/types/fraction"
      via: "imports ProblemType"
      pattern: "import.*ProblemType.*from"
    - from: "src/components/DifficultySelector.tsx"
      to: "@/types/fraction"
      via: "imports Difficulty"
      pattern: "import.*Difficulty.*from"
---

<objective>
Create mode and difficulty selector components for practice mode selection.

Purpose: Enable students to choose which comparison method to practice (MODE-01 to MODE-04) and at what difficulty level (MODE-05).
Output: Two reusable selector components ready for page integration.
</objective>

<execution_context>
@C:\Users\limfe\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\limfe\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/types/fraction.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ModeSelector component</name>
  <files>src/components/ModeSelector.tsx</files>
  <action>
Create a client component that displays 4 mode buttons in a grid layout.

Props:
- selectedMode: ProblemType | "mixed" | null
- onModeSelect: (mode: ProblemType | "mixed") => void

Buttons:
1. "Same Numerator" -> value "same-numerator"
2. "Same Denominator" -> value "same-denominator"
3. "Different Both" -> value "different-both"
4. "Mixed" -> value "mixed"

Styling (Tailwind):
- Grid: 2x2 on mobile, 4 cols on md+
- Each button: rounded-lg, p-4, min-h-[60px]
- Unselected: bg-gray-100, border-gray-300
- Selected: bg-blue-500, text-white, ring-2 ring-blue-300
- Hover: bg-gray-200 (unselected), bg-blue-600 (selected)
- Focus-visible: ring-2 ring-offset-2

Use "use client" directive. Import ProblemType from @/types/fraction.
  </action>
  <verify>TypeScript compiles: npx tsc --noEmit</verify>
  <done>ModeSelector exports and renders 4 mode buttons with selection state</done>
</task>

<task type="auto">
  <name>Task 2: Create DifficultySelector component</name>
  <files>src/components/DifficultySelector.tsx</files>
  <action>
Create a client component that displays 3 difficulty buttons in a row.

Props:
- selectedDifficulty: Difficulty | null
- onDifficultySelect: (difficulty: Difficulty) => void

Buttons:
1. "Easy" -> value "easy"
2. "Medium" -> value "medium"
3. "Hard" -> value "hard"

Styling (Tailwind):
- Flex row with gap-3, justify-center
- Each button: rounded-lg, px-6, py-3, font-medium
- Unselected: bg-gray-100, text-gray-700, border border-gray-300
- Selected: bg-green-500, text-white
- Hover: bg-gray-200 (unselected), bg-green-600 (selected)
- Focus-visible: ring-2 ring-offset-2

Difficulty-specific colors for selected state (optional enhancement):
- Easy: bg-green-500
- Medium: bg-yellow-500
- Hard: bg-red-500

Use "use client" directive. Import Difficulty from @/types/fraction.
  </action>
  <verify>TypeScript compiles: npx tsc --noEmit</verify>
  <done>DifficultySelector exports and renders 3 difficulty buttons with selection state</done>
</task>

</tasks>

<verification>
1. npx tsc --noEmit - no type errors
2. Both components export named exports
3. Both import types from @/types/fraction
</verification>

<success_criteria>
- ModeSelector renders 4 buttons for all problem types + mixed
- DifficultySelector renders 3 buttons for easy/medium/hard
- Both handle selection state visually
- Both are touch-friendly (min 44px touch targets)
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-practice-modes/03-01-SUMMARY.md`
</output>
