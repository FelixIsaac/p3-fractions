---
phase: 04-advanced-visuals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/BenchmarkComparisonView.tsx
  - src/components/PracticeSession.tsx
autonomous: true

must_haves:
  truths:
    - "Student sees 1/2 benchmark bar in Method 3 (different-both) mode"
    - "Three bars display side-by-side: 1/2 reference + fraction A + fraction B"
    - "Benchmark bar clearly labeled as 1/2 reference"
  artifacts:
    - path: "src/components/BenchmarkComparisonView.tsx"
      provides: "Three-bar comparison layout with 1/2 benchmark"
      exports: ["BenchmarkComparisonView"]
    - path: "src/components/PracticeSession.tsx"
      provides: "Integration showing benchmark view for different-both"
      contains: "BenchmarkComparisonView"
  key_links:
    - from: "src/components/PracticeSession.tsx"
      to: "src/components/BenchmarkComparisonView.tsx"
      via: "conditional render for different-both type"
      pattern: "type.*different-both.*BenchmarkComparisonView"
---

<objective>
Create 1/2 benchmark reference bar display for Method 3 (different-both) comparisons.

Purpose: VIS-06 requires students see a 1/2 reference bar when comparing fractions using the benchmark method. This visual anchor helps students determine if each fraction is greater or less than 1/2.

Output: BenchmarkComparisonView component showing three bars side-by-side, integrated into PracticeSession for different-both problems.
</objective>

<execution_context>
@C:\Users\limfe\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\limfe\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/components/FractionBar.tsx
@src/components/PracticeSession.tsx
@src/types/fraction.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BenchmarkComparisonView component</name>
  <files>src/components/BenchmarkComparisonView.tsx</files>
  <action>
Create new component that displays three FractionBar components vertically:
1. Top: 1/2 benchmark reference bar (always shows 1/2 with 1 segment shaded)
2. Middle: Fraction A bar (shows fractionA with numerator segments shaded)
3. Bottom: Fraction B bar (shows fractionB with numerator segments shaded)

Props interface:
- fractionA: Fraction
- fractionB: Fraction
- width?: number (default 500)

Layout requirements:
- Stack vertically with consistent gap (gap-4)
- Each bar row has: label on left (fixed width ~50px), bar in flex-1 container, fraction text on right
- Benchmark row label: "1/2" with subtle styling (text-gray-500)
- A/B rows: bold "A" and "B" labels matching PracticeSession style
- All bars same width for visual alignment

Use existing FractionBar component with interactive={false} and pre-computed shadedSegments.

Do NOT add animation logic in this task - that's Plan 02.
  </action>
  <verify>
npm run build compiles without errors.
Component renders three bars when given two fractions.
  </verify>
  <done>
BenchmarkComparisonView exports and renders three vertically-stacked FractionBars with proper labels.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate benchmark view into PracticeSession</name>
  <files>src/components/PracticeSession.tsx</files>
  <action>
Modify PracticeSession to conditionally render BenchmarkComparisonView when problem type is "different-both":

1. Import BenchmarkComparisonView
2. In the JSX where fraction bars are rendered:
   - If currentProblem.type === "different-both", render BenchmarkComparisonView
   - Otherwise, keep existing two-bar layout

Keep the existing two-bar layout code intact for same-numerator and same-denominator modes - do NOT refactor it into the new component. The existing layout works and is proven.

Ensure the "Which fraction is bigger?" prompt and AnswerButtons remain below the bars regardless of which layout is shown.
  </action>
  <verify>
npm run dev starts without errors.
Navigate to practice mode, select "Different Both" - should see three bars (1/2 + A + B).
Navigate to "Same Numerator" or "Same Denominator" - should see only two bars (A + B).
  </verify>
  <done>
Different-both mode shows 1/2 benchmark reference bar.
Other modes continue showing two-bar comparison.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `npm run dev` starts without errors
3. Manual verification: Different-both mode shows three bars with 1/2 reference
4. Manual verification: Same-numerator and same-denominator modes show two bars only
</verification>

<success_criteria>
- Student sees three bars side-by-side in Method 3: 1/2 reference + both fractions
- 1/2 benchmark bar is clearly distinguishable as the reference
- Other practice modes unaffected (still show two bars)
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-visuals/04-01-SUMMARY.md`
</output>
