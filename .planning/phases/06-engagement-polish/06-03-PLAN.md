---
phase: 06-engagement-polish
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - src/app/page.tsx
  - src/components/PracticeSession.tsx
  - src/components/ModeSelector.tsx
  - src/components/DifficultySelector.tsx
  - src/components/AnswerButtons.tsx
  - src/app/globals.css
autonomous: false

must_haves:
  truths:
    - "Student sees only one problem at a time"
    - "No distracting decorative animations"
    - "No performance timers visible"
    - "Touch targets are large and well-spaced"
    - "Interface has clean, high-contrast design"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Resume capability on app load"
    - path: "src/components/AnswerButtons.tsx"
      provides: "Large touch-friendly buttons"
    - path: "src/app/globals.css"
      provides: "High-contrast base styles"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/lib/progress/storage.ts"
      via: "Check for paused session on load"
      pattern: "loadProgress|resumeSession"
---

<objective>
UX polish for ADHD learner: single-focus interface, minimal clutter, touch-friendly.

Purpose: Optimize experience for learner who zones out with clutter, needs direct interaction (UX-01 through UX-06).
Output: Clean, focused interface verified on tablet and laptop.
</objective>

<execution_context>
@C:\Users\limfe\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\limfe\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-engagement-polish/06-02-SUMMARY.md
@src/app/page.tsx
@src/components/PracticeSession.tsx
@src/components/ModeSelector.tsx
@src/components/DifficultySelector.tsx
@src/components/AnswerButtons.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Touch target and contrast audit</name>
  <files>src/components/AnswerButtons.tsx, src/components/ModeSelector.tsx, src/components/DifficultySelector.tsx, src/app/globals.css</files>
  <action>
Audit and update all interactive elements:

AnswerButtons.tsx:
- Ensure min-h-[56px] min-w-[140px] (already close, verify)
- Increase gap between buttons to gap-6 for fat finger safety
- Verify text contrast ratio meets WCAG AA (4.5:1)

ModeSelector.tsx:
- Mode buttons: min-h-[64px] min-w-[200px]
- Increase padding to py-5 px-8
- Add gap-6 between mode options
- High contrast: dark text on light bg, clear borders

DifficultySelector.tsx:
- Same sizing as ModeSelector
- Ensure Easy/Medium/Hard clearly distinguishable
- No color-only differentiation (add text labels)

globals.css additions:
```css
/* High contrast focus states */
:focus-visible {
  outline: 3px solid #2563eb;
  outline-offset: 2px;
}

/* Ensure no tiny tap targets */
button, [role="button"] {
  min-height: 44px;
  min-width: 44px;
}

/* Remove any decorative animations */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
}
```

Remove any decorative animations found (spinning, bouncing, etc). Keep only functional feedback animations (brief color change on click).
  </action>
  <verify>Inspect elements in dev tools, verify all buttons meet size requirements</verify>
  <done>All touch targets >= 44px, high contrast throughout, no decorative animations</done>
</task>

<task type="auto">
  <name>Task 2: Resume paused session on app load</name>
  <files>src/app/page.tsx</files>
  <action>
Update page.tsx to check for paused sessions:

1. On mount, call loadProgress() to check for paused session
2. If paused session exists, show "Resume Session?" prompt:
   - Display mode and difficulty of paused session
   - Show progress (e.g., "5 of 10 problems completed")
   - Two buttons: "Resume" and "Start Fresh"
3. Resume: set mode/difficulty from stored, set isPlaying true, pass resumeIndex to PracticeSession
4. Start Fresh: clear stored session, show normal mode selector

Add prop to PracticeSession: resumeFromIndex?: number
- If provided, start from that problem index instead of 0
- Generate same problem sequence (use seeded random with session key)

Ensure single-focus maintained:
- Only one view visible at a time
- No split attention between multiple UI sections
- Clear visual hierarchy (one primary action per screen)
  </action>
  <verify>
Manually test:
1. Start session, answer 3 problems, click Pause, close browser
2. Reopen browser - see "Resume Session?" prompt
3. Click Resume - continue from problem 4
  </verify>
  <done>App checks for and offers to resume paused sessions on load</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete engagement and UX polish for ADHD-friendly interface</what-built>
  <how-to-verify>
Test on both laptop and tablet (or tablet emulator):

1. **Single Focus (UX-01):** Only one problem visible at a time, no side panels
2. **Minimal Clutter (UX-02):** No decorative animations, spinning elements, or flashy effects
3. **No Timers (UX-03):** Confirm no countdown clocks or time pressure indicators anywhere
4. **Touch Targets (UX-04):** All buttons comfortable to tap (try with finger, not mouse)
5. **High Contrast (UX-05):** Text readable, buttons clearly visible, borders distinct
6. **Pause/Resume (UX-06):** Pause mid-session, close browser, reopen, resume works

7. **Streak Counter (ENG-01):** Visible, updates correctly
8. **Progress Bar (ENG-02):** Shows completion progress
9. **Session Summary (ENG-03):** Appears after 10 problems
10. **Persistence (ENG-04):** Progress survives browser close/reopen
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All 10 requirements (ENG-01-04, UX-01-06) pass manual verification
- `npm run build` succeeds
- Lighthouse accessibility score >= 90
</verification>

<success_criteria>
- Interface feels calm and focused, not overwhelming
- Tutor can use during session without student losing focus
- Student can independently resume practice between sessions
- All interactions work equally well on touch and mouse
</success_criteria>

<output>
After completion, create `.planning/phases/06-engagement-polish/06-03-SUMMARY.md`
</output>
