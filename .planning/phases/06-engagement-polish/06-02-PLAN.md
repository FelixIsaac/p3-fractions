---
phase: 06-engagement-polish
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/StreakCounter.tsx
  - src/components/ProgressBar.tsx
  - src/components/SessionSummary.tsx
  - src/components/PracticeSession.tsx
autonomous: true

must_haves:
  truths:
    - "Student sees streak counter increase with correct answers"
    - "Student sees progress bar fill as problems completed"
    - "Student sees session summary at end"
  artifacts:
    - path: "src/components/StreakCounter.tsx"
      provides: "Visual streak display"
      exports: ["StreakCounter"]
    - path: "src/components/ProgressBar.tsx"
      provides: "Progress bar for level completion"
      exports: ["ProgressBar"]
    - path: "src/components/SessionSummary.tsx"
      provides: "End-of-session results display"
      exports: ["SessionSummary"]
  key_links:
    - from: "src/components/PracticeSession.tsx"
      to: "src/hooks/useProgress.ts"
      via: "useProgress hook call"
      pattern: "useProgress\\("
    - from: "src/components/PracticeSession.tsx"
      to: "src/components/StreakCounter.tsx"
      via: "JSX render"
      pattern: "<StreakCounter"
---

<objective>
Create engagement UI components and integrate with PracticeSession.

Purpose: Visible progress indicators (ENG-01, ENG-02, ENG-03) that motivate learner without pressure.
Output: Streak counter, progress bar, session summary integrated into practice flow.
</objective>

<execution_context>
@C:\Users\limfe\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\limfe\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-engagement-polish/06-01-SUMMARY.md
@src/components/PracticeSession.tsx
@src/hooks/useProgress.ts
@src/types/progress.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Engagement UI components</name>
  <files>src/components/StreakCounter.tsx, src/components/ProgressBar.tsx, src/components/SessionSummary.tsx</files>
  <action>
Create src/components/StreakCounter.tsx:
- Props: { currentStreak: number; bestStreak: number }
- Display current streak prominently (large number)
- Show "Best: X" smaller below
- Use encouraging colors (not red for low streaks)
- Minimal design - just the numbers, no decorative elements
- Animation on increment (subtle scale pulse, no flashy effects per UX-02)

Create src/components/ProgressBar.tsx:
- Props: { current: number; total: number; label?: string }
- Horizontal bar showing completion percentage
- Clean design with rounded corners
- High contrast (dark fill on light background per UX-05)
- Show "X of Y" text below bar
- Total defaults to 10 problems per level

Create src/components/SessionSummary.tsx:
- Props: { problems: number; correct: number; streak: number; onContinue: () => void; onExit: () => void }
- Display final stats: problems attempted, correct answers, best streak
- Calculate and show percentage correct
- Encouraging message regardless of score
- Two buttons: "Continue Practicing" and "Back to Menu"
- Large touch targets (min 48px per UX-04)
  </action>
  <verify>Components render without errors, TypeScript compiles</verify>
  <done>Three engagement components created with ADHD-friendly minimal design</done>
</task>

<task type="auto">
  <name>Task 2: Integrate into PracticeSession</name>
  <files>src/components/PracticeSession.tsx</files>
  <action>
Update PracticeSession.tsx:
1. Import useProgress hook and new components
2. Initialize useProgress with mode and difficulty
3. Call recordAnswer(isCorrect) in handleAnswer
4. Add StreakCounter to header area (next to mode/difficulty label)
5. Add ProgressBar below header (track problems in current session)
6. Add session end detection: after 10 problems OR user clicks "End Session"
7. When session ends, show SessionSummary instead of problem view
8. Handle SessionSummary callbacks:
   - onContinue: reset progress, generate new problem
   - onExit: call existing onExit prop

Layout changes:
- Move "Back" button to left
- Add "Pause" button to right (saves state, shows confirmation)
- StreakCounter in center-right of header
- ProgressBar spans full width below header
- Keep single problem visible at a time (already done)

Pause behavior:
- Click Pause -> pauseSession() -> show "Session Paused" overlay
- Overlay has "Resume" and "Exit" buttons
- Resume continues from same problem
  </action>
  <verify>
Run `npm run dev`, start practice session:
- Streak counter visible and updates
- Progress bar fills as problems answered
- After 10 problems, see session summary
- Pause/resume works
  </verify>
  <done>PracticeSession shows engagement indicators and supports pause/resume</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds without errors
- Practice session displays streak and progress
- Session summary appears after 10 problems
- Pause saves state, resume continues correctly
</verification>

<success_criteria>
- Streak counter increments on correct, resets on wrong
- Progress bar shows X/10 problems completed
- Session summary displays encouraging stats
- Pause/resume preserves exact problem state
</success_criteria>

<output>
After completion, create `.planning/phases/06-engagement-polish/06-02-SUMMARY.md`
</output>
